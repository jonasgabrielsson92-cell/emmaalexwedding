import { useEffect, useState } from 'react';
const photos = [{ src: '/images/frieriet-alperna.jpg', alt: 'Förlovningsringen i Alperna', className: 'g-tall' }, { src: '/images/emma-ostron.jpg', alt: 'Emma med ostron i skärgården' }, { src: '/images/elsie.jpg', alt: 'Lilla Elsie' }, { src: '/images/alexander-ostron.jpg', alt: 'Alexander med ostron i skärgården', className: 'g-tall' }];
export default function Gallery() { const [selected, setSelected] = useState(null); useEffect(() => { const close = (event) => event.key === 'Escape' && setSelected(null); window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, []); return <section className="alt" id="galleri"><div className="wrap"><div className="section-head"><p className="kicker">Galleri</p><h2>Ögonblick</h2><p className="lead">Här fyller vi på med bilder före och efter den stora dagen.</p></div><div className="gallery">{photos.map((photo) => <button className={`ph ${photo.className || ''}`} key={photo.src} onClick={() => setSelected(photo)}><img src={photo.src} alt={photo.alt} /></button>)}</div></div>{selected && <div className="lightbox open" role="dialog" aria-modal="true" aria-label={selected.alt} onClick={() => setSelected(null)}><button className="lightbox__close" onClick={() => setSelected(null)} aria-label="Stäng">×</button><div className="ph" onClick={(event) => event.stopPropagation()}><img src={selected.src} alt={selected.alt} /></div></div>}</section>; }
